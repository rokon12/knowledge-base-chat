package ca.bazlur.config;

/**
 * Enum representing the different AI providers supported by the application.
 */
public enum AIProvider {
    /**
     * OpenAI provider (GPT models)
     */
    OPENAI,
    
    /**
     * Ollama provider (local models)
     */
    OLLAMA
}